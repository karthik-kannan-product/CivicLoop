name: civicloop

x-app: &app
  build:
    context: ${CIVICLOOP_BUILD_CONTEXT:-.}
    labels:
      org.opencontainers.image.revision: ${CIVICLOOP_APPLICATION_SHA:-unbound-local-build}
  image: civicloop:local
  env_file:
    - .env
  restart: unless-stopped
  read_only: true
  tmpfs:
    - /tmp:mode=1777

services:
  db:
    image: ${POSTGRES_IMAGE:-docker.io/library/postgres:17.10-alpine@sha256:742f40ea20b9ff2ff31db5458d127452988a2164df9e17441e191f3b72252193}
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 3s
      retries: 20
    volumes:
      - postgres-data:/var/lib/postgresql/data

  valkey:
    image: valkey/valkey:8.1-alpine
    command: ["valkey-server", "--save", "", "--appendonly", "no"]
    healthcheck:
      test: ["CMD", "valkey-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 20

  migrate:
    <<: *app
    command: ["manage", "migrate", "--noinput"]
    restart: "no"
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - ${CIVICLOOP_IDENTITY_KEY_HOST_PATH:?Set an absolute host identity-key path}:/run/secrets/civicloop-identity-keyring.json:ro

  web:
    <<: *app
    command: ["web"]
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - ${CIVICLOOP_IDENTITY_KEY_HOST_PATH:?Set an absolute host identity-key path}:/run/secrets/civicloop-identity-keyring.json:ro
    depends_on:
      migrate:
        condition: service_completed_successfully
      valkey:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/api/v1/health/live')"]
      interval: 10s
      timeout: 3s
      retries: 12

  worker:
    <<: *app
    command: ["worker"]
    environment:
      CIVICLOOP_ADMIN_IDENTITY_ENABLED: "false"
      CIVICLOOP_IDENTITY_KEY_FILE: ""
    depends_on:
      migrate:
        condition: service_completed_successfully
      valkey:
        condition: service_healthy

  scheduler:
    <<: *app
    command: ["scheduler"]
    environment:
      CIVICLOOP_ADMIN_IDENTITY_ENABLED: "false"
    depends_on:
      migrate:
        condition: service_completed_successfully
      valkey:
        condition: service_healthy
volumes:
  postgres-data:
    name: ${POSTGRES_VOLUME_NAME:-civicloop_postgres-data}
